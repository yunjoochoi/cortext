[project]
name = "cortext"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "diffusers>=0.36.0",
    "torch>=2.10.0",
]
